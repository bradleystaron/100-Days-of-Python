# A list of fruits
fruits = ["Apple", "Peach", "Pear"]

# Creates a variable "i" for each fruit within fruits to be stored into
# Then prints the corresponding variable at each iteration of the list
for i in fruits:
    print(i)
    print(i + "Pie")
    print(fruits)